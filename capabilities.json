{
    "version": "1.0",
    "name": "AirData",
    "description": "Ingests drone flight telemetry from AirData and posts aircraft positions to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "AirData posts drone positions to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls AirData for updated drone flight telemetry on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(1 minute)"
                }
            }
        }
    }
}
